import { ExtractLastItemPipe } from './extract-last-item.pipe';

describe('ExtractLastItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractLastItemPipe();
    expect(pipe).toBeTruthy();
  });
});
